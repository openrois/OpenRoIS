{
  "$comment": "GENERATED FROM interfaces/python: DO NOT EDIT. Run scripts/export_schema.py.",
  "$defs": {
    "ConditionT": {
      "type": "string"
    },
    "EventType": {
      "type": "string"
    },
    "RoISIdentifier": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "Request to subscribe to events matching a condition.\n\nMaps to EventIF.subscribe(event_type, condition, subscribe_id).",
  "properties": {
    "component_ref": {
      "$ref": "#/$defs/RoISIdentifier",
      "description": "Target component instance ref"
    },
    "event_type": {
      "$ref": "#/$defs/EventType",
      "description": "Event type to subscribe to"
    },
    "condition": {
      "$ref": "#/$defs/ConditionT",
      "default": "",
      "description": "Optional filter expression for the subscription"
    }
  },
  "required": [
    "component_ref",
    "event_type"
  ],
  "title": "SubscribeRequest",
  "type": "object"
}